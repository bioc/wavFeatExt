% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.wavFeatExtClassifier.R
\name{plot.wavFeatExtClassifier}
\alias{plot.wavFeatExtClassifier}
\title{Plot Classification Results from \code{classifyWavFeatExt}}
\usage{
\method{plot}{wavFeatExtClassifier}(x, type = c("CE", "AUC"), adjust = TRUE, ...)
}
\arguments{
\item{x}{The result object returned by \code{\link[=classifyWavFeatExt]{classifyWavFeatExt()}}, containing at
least the components \code{CE}, \code{AUC}, and \code{method}.}

\item{type}{Character string specifying which performance measure to plot.
One of \code{"CE"} for misclassification error, or \code{"AUC"} for the area under
the ROC curve.}

\item{adjust}{Logical flag reserved for future extensions of the plotting
function. Currently not used.}

\item{...}{Additional graphical parameters passed to
\code{\link[graphics:boxplot]{graphics::boxplot()}}.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Create boxplots of cross-validated performance measures (classification
error or AUC) for wavelet-based feature sets used in
\code{\link[=classifyWavFeatExt]{classifyWavFeatExt()}}, namely detail and scaling coefficients,
and the original/segmented data.
}
\details{
It is assumed that \code{\link[=classifyWavFeatExt]{classifyWavFeatExt()}} assigns column names to its \code{CE}
and \code{AUC} matrices as follows:
\itemize{
\item Columns beginning with \code{"D"} correspond to wavelet detail coefficients.
\item Columns beginning with \code{"S"} correspond to wavelet scaling coefficients.
\item The column \code{"seg"} corresponds to the original (or segmented) data.
}

The corresponding columns are displayed as boxplots summarising
cross-validated performance across replications.

Two horizontal reference lines are added:
\itemize{
\item Red dashed line: median performance of \code{"seg"}
\item Blue dotted line: best median performance among all features
}
}
\examples{
set.seed(10)

## Small simulated data for fast execution
sim.dat <- simulateCNA(
  n.obs = 20,
  p = 32,
  n.sim = 1,
  n.block = 8,
  verbose = FALSE
)

## Extract wavelet features
det.coef <- wavFeatExt(sim.dat, type = "detail")
sca.coef <- wavFeatExt(sim.dat, type = "scaling")

## Binary response
y <- factor(rep(c("Group1", "Group2"), each = 10))

## Classification (fast method)
res <- classifyWavFeatExt(
  sim.dat,
  y,
  det.coef,
  sca.coef,
  method = "KNN",
  k = 5,
  ite = 1
)

## Plot results
plot(res, type = "CE")
plot(res, type = "AUC")

}
\seealso{
\code{\link[=classifyWavFeatExt]{classifyWavFeatExt()}},
\code{\link[=wavFeatExt]{wavFeatExt()}},
\code{\link[=simulateCNA]{simulateCNA()}}
}
